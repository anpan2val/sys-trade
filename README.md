# local development
## Docker Compose
```
# build
$ docker compose build --no-cache

# start
$ docker-compose up -d
```

## Local server

```
# enter container
$ docker compose exec dev-app bash

# start server
$ npm run dev
```

## access localhost
http://localhost
